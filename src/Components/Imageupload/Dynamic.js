// import { render } from "@testing-library/react"

// export const dynamicHtml = () =>{
//     return(
//         <span key={this.props.ind} ref={this.dynamicRef} style={{ position: 'absolute' }} id={item.name + this.props.ind}>{this.props.item}</span>
//     )
// }
